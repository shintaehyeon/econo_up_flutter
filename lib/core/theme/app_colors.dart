// lib/core/theme/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // 메인 배경 및 레이아웃
  static const Color background = Color(0xFFF5F7FB);
  static const Color paper = Color(0xFFFFFFFF);
  static const Color paperSoft = Color(0xFFF8FBFF);
  
  // 텍스트 및 기본 잉크
  static const Color ink = Color(0xFF172033);
  static const Color muted = Color(0xFF5D687C);
  
  // 보더라인 및 구분선
  static const Color line = Color(0xFFD8E0EE);
  static const Color lineStrong = Color(0xFFBCC9DF);
  
  // 어두운 네비게이션 테마
  static const Color nav = Color(0xFF0F172A);
  static const Color navSoft = Color(0xFF18243D);
  
  // 브랜드 메인 (Blue) - 학습, 단추 등 주요 포인트
  static const Color brand = Color(0xFF1463FF);
  static const Color brandSoft = Color(0xFFE8F0FF);
  
  // 민트 (Mint) - 성공, MVP 태그, 완료 항목
  static const Color mint = Color(0xFF0F9F74);
  static const Color mintSoft = Color(0xFFE5FBF3);
  
  // 골드 (Gold) - 경고, 부활권, 연속 스트릭 충전
  static const Color gold = Color(0xFFB05B00);
  static const Color goldSoft = Color(0xFFFFF0D9);
  
  // 바이올렛 (Violet) - Post-MVP, 소셜, 리그, 배틀
  static const Color violet = Color(0xFF6656D9);
  static const Color violetSoft = Color(0xFFEEEAFF);
  
  // 덴저 (Danger/Red) - 오류, 이탈, 하트 0개 위기 상황
  static const Color danger = Color(0xFFBC3347);
  static const Color dangerSoft = Color(0xFFFFF0F2);
}
